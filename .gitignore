# AI Framework - Project .gitignore
# Auto-installed by AI Framework plugin
# Prevents framework files from being committed to your project repository

# ============================================================================
# FRAMEWORK FILES (installed but not committed)
# ============================================================================

# Framework internals (configuration, runtime)
/.claude/*
# Project memory rules ARE tracked (generated by /project-init, reviewable in PRs)
!/.claude/rules/
# Manifest checksums cache (runtime, not project state)
/.claude/rules/.manifest-checksums
# Framework project files (ignored by default)
/CLAUDE.md

# ============================================================================
# FRAMEWORK RUNTIME (logs, databases, temp files)
# ============================================================================

# Hook databases (notifications, tracking)
/hooks/*.db

# ============================================================================
# COMMON PROJECT FILES (add your own)
# ============================================================================

# Environment variables
.env
.env.local
.env.*.local

# OS files
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db

# Editor directories
.vscode/
.idea/
.cursor-settings.json
*.swp
*.swo
*~

# Dependencies
node_modules/
vendor/
jspm_packages/
.npm

# Python
__pycache__/
*.pyc
*.py[cod]
*$py.class
*.so
.pytest_cache/

# Test coverage
.coverage
coverage/
*.lcov
.nyc_output

# TypeScript
*.tsbuildinfo

# Build outputs
dist/
build/
.next/
.expo/
.parcel-cache

# Logs
*.log

# Backup and temporary files
*.backup
*.bak
*-backup.*
*-fixed.*
*-temp.*
*.orig
backup/
backups/
tmp/
temp/
*.tmp

# Local configuration overrides
settings.local.json
config.local.json

# Playwright MCP test artifacts (if using Playwright MCP server)
test-results/
playwright-report/
blob-report/
.local-browsers/
drivers/
.auth/
*.auth.json

# VitePress auto-generated files
human-handbook/.vitepress/dist/
human-handbook/.vitepress/cache/


# Planning & design documents (working files, not for version control)
/docs/plans/
/docs/research/

# Ralph orchestrator runtime
/.ralph/

# AI Framework runtime files (auto-added)
/hooks/__pycache__/
