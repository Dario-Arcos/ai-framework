{
  "$comment": "Copy to project root as .mcp.json — Full docs: human-handbook/docs/mcp-servers.md",
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp@latest"],
      "$description": "Browser automation, E2E testing, screenshots"
    },
    "shadcn": {
      "command": "npx",
      "args": ["shadcn@latest", "mcp"],
      "$description": "Shadcn/ui component library integration"
    },
    "mobile-mcp": {
      "command": "npx",
      "args": ["-y", "@mobilenext/mobile-mcp@latest"],
      "$description": "Mobile automation: iOS/Android simulators, emulators, real devices. Interactive debugging and UI exploration. Requires: Node.js 22+, Xcode CLI (iOS), Android Platform Tools (Android)"
    },
    "maestro": {
      "command": "maestro",
      "args": ["mcp"],
      "$description": "Mobile E2E testing: YAML flows, auto-healing, AI assertions. Supports React Native, Expo, Flutter. Requires: Java 17+, maestro CLI (curl -fsSL https://get.maestro.mobile.dev | bash)"
    }
  }
}
