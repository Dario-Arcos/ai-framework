{
  "$comment": "MCPs base del framework. Agregar MCPs adicionales según necesidad del proyecto.",
  "mcpServers": {
    "context7": {
      "type": "http",
      "url": "https://mcp.context7.com/mcp",
      "$description": "Documentation lookup for libraries and frameworks (no auth required)"
    },
    "mobile-mcp": {
      "command": "npx",
      "args": ["-y", "@mobilenext/mobile-mcp@latest"],
      "$description": "Mobile automation, UI debugging, accessibility trees (iOS/Android)"
    },
    "maestro": {
      "command": "maestro",
      "args": ["mcp"],
      "$description": "Mobile E2E testing, YAML flows, AI assertions"
    }
  }
}
