# Stack

**Runtime**: Node.js (ES modules, `"type": "module"`) + Python 3.8+ (hooks) + Bash (scripts)
**Framework**: Claude Code Plugin API v5.1.2
**Type**: plugin (single package, not monorepo)

## Critical Dependencies
- vitepress (^1.6.4) — documentation site generator
- mermaid (^11.12.2) — diagram rendering
- vitepress-plugin-mermaid (^2.0.17) — mermaid integration
- context7 MCP (HTTP) — external API documentation retrieval

## Scripts
- `npm run docs:dev` — start VitePress dev server
- `npm run docs:build` — compile documentation to HTML
- `npm run docs:preview` — preview built documentation
- `npm version [major|minor|patch]` — bump version + sync across all manifests (sync-versions.cjs)

## CI/CD (GitHub Actions)
- `claude-code-review.yml` — automated security/code review on PRs (Opus model)
- `deploy.yml` — VitePress → GitHub Pages on push to main
- `sync-marketplace.yml` — version sync to marketplace on tag push
- `sync-develop.yml` — mirror main → develop

---
*Auto-generated by /project-init on 2026-02-13*
