# Architecture

## Entry Point
`.claude-plugin/plugin.json` → Plugin manifest defining framework capabilities

## Layer Structure
```
agents/          # 20 specialized AI agents (code-reviewer, security-reviewer, etc.)
commands/        # 26 slash commands (/git-commit, /project-init, etc.)
skills/          # 27 reusable skill modules (TDD, debugging, etc.)
hooks/           # 8 lifecycle hooks (session-start, security-guard, etc.)
template/        # Project scaffolding templates
docs/            # Documentation and claude-rules
human-handbook/  # VitePress documentation site
```

## Plugin Components
- **Agents**: Specialized AI personas for specific tasks
- **Commands**: User-invokable slash commands
- **Skills**: Process documentation and workflows
- **Hooks**: Lifecycle events (session start, tool calls, prompts)

## Data Flow
1. User invokes Claude Code in project
2. Session-start hook syncs rules from `docs/claude-rules/` to `.claude/rules/`
3. Claude loads CLAUDE.md + rules automatically
4. User invokes commands/skills as needed
5. Security hooks validate dangerous operations

---
*Auto-generated by /project-init on 2025-12-11*
